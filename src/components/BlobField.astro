<div class="blob-field" aria-hidden="true">
  <span class="blob blob-pink one"></span>
  <span class="blob blob-lime two"></span>
  <span class="blob blob-pink three"></span>
  <span class="grid-overlay"></span>
</div>

<style>
  .blob-field {
    position: absolute;
    inset: 0;
    overflow: hidden;
    z-index: 0;
    pointer-events: none;
  }

  .blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(70px);
    opacity: 0.55;
    mix-blend-mode: screen;
  }

  .blob-pink {
    background: radial-gradient(circle at 30% 30%, var(--pink-light), var(--pink) 70%);
  }

  .blob-lime {
    background: radial-gradient(circle at 30% 30%, #d4ff4d, var(--lime) 70%);
  }

  .one {
    width: 46vw;
    height: 46vw;
    top: -10%;
    left: -12%;
    animation: float1 18s ease-in-out infinite;
  }

  .two {
    width: 38vw;
    height: 38vw;
    bottom: -12%;
    right: -8%;
    animation: float2 22s ease-in-out infinite;
  }

  .three {
    width: 28vw;
    height: 28vw;
    top: 40%;
    right: 20%;
    animation: float3 16s ease-in-out infinite;
  }

  .grid-overlay {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(rgba(255, 255, 255, 0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.035) 1px, transparent 1px);
    background-size: 42px 42px;
  }

  @keyframes float1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(6vw, 8vh) scale(1.15); }
  }
  @keyframes float2 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(-5vw, -6vh) scale(1.1); }
  }
  @keyframes float3 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(-4vw, 5vh) scale(1.2); }
  }

  @media (prefers-reduced-motion: reduce) {
    .blob { animation: none !important; }
  }
</style>
